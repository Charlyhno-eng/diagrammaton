import type { Diagram, DiagramEdge, DiagramNode, Side } from '../../features/diagram/domain/diagram'
import { getTechnology } from '../../features/diagram/domain/technologyCatalog'
import { getElectronicComponent } from '../../features/diagram/domain/electronicCatalog'

const esc = (value = '') => value.replace(/[&<>"']/g, char => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#039;' }[char]!))
const width = (node: DiagramNode) => node.width ?? 190
const height = (node: DiagramNode) => node.height ?? 100
const endpoint = (node: DiagramNode, side: Side) => side === 'top' ? { x: node.x + width(node) / 2, y: node.y } : side === 'bottom' ? { x: node.x + width(node) / 2, y: node.y + height(node) } : side === 'left' ? { x: node.x, y: node.y + height(node) / 2 } : { x: node.x + width(node), y: node.y + height(node) / 2 }
const pathFor = (edge: DiagramEdge, from: DiagramNode, to: DiagramNode) => {
  const a = endpoint(from, edge.fromSide), b = endpoint(to, edge.toSide)
  if (edge.route === 'straight') return `M ${a.x} ${a.y} L ${b.x} ${b.y}`
  if (edge.route === 'curve') {
    const horizontal = edge.fromSide === 'left' || edge.fromSide === 'right', distance = horizontal ? Math.max(80, Math.abs(b.x - a.x) * .42) : Math.max(70, Math.abs(b.y - a.y) * .42)
    const vector = (side: Side) => side === 'left' ? [-1, 0] : side === 'right' ? [1, 0] : side === 'top' ? [0, -1] : [0, 1]
    const av = vector(edge.fromSide), bv = vector(edge.toSide)
    return `M ${a.x} ${a.y} C ${a.x + av[0] * distance} ${a.y + av[1] * distance}, ${b.x + bv[0] * distance} ${b.y + bv[1] * distance}, ${b.x} ${b.y}`
  }
  if ((edge.fromSide === 'left' || edge.fromSide === 'right') && (edge.toSide === 'left' || edge.toSide === 'right')) { const x = (a.x + b.x) / 2; return `M ${a.x} ${a.y} H ${x} V ${b.y} H ${b.x}` }
  const y = (a.y + b.y) / 2; return `M ${a.x} ${a.y} V ${y} H ${b.x} V ${b.y}`
}

function renderNode(node: DiagramNode) {
  const items = node.items?.map(item => `<span>${esc(item)}</span>`).join('') ?? ''
  const technology = getTechnology(node.technology), logoColor = technology?.icon.hex === '000000' ? '#eef2f5' : `#${technology?.icon.hex}`
  const electronic = getElectronicComponent(node.electronic)
  const icon = technology ? `<i class="brand" style="color:${logoColor}"><svg viewBox="0 0 24 24" role="img" aria-label="${esc(technology.name)}"><path fill="currentColor" d="${technology.icon.path}"/></svg></i>` : electronic ? `<i class="brand electronic"><svg viewBox="0 0 24 24" role="img" aria-label="${esc(electronic.name)}">${electronic.svg}</svg></i>` : node.icon ? `<i>${esc(node.icon.slice(0, 2).toUpperCase())}</i>` : ''
  const visual = node.kind === 'platform' ? '<div class="platform-visual"><i></i><i></i><i></i><i></i><i></i><i></i></div>' : node.kind === 'database' ? '<div class="database-stack"><i></i><i></i><i></i></div>' : node.kind === 'group' ? '<div class="group-grid"></div>' : ''
  return `<article class="node ${esc(node.kind)} motion-${esc(node.animation)}" style="left:${node.x}px;top:${node.y}px;width:${width(node)}px;height:${height(node)}px;--tone:${esc(node.color)};--ink:${esc(node.textColor)}">
    <div class="heading">${icon}<div>${node.eyebrow ? `<em>${esc(node.eyebrow)}</em>` : ''}<h2>${esc(node.title)}</h2></div>${node.badge ? `<b>${esc(node.badge)}</b>` : ''}</div>
    <p>${esc(node.subtitle)}</p>${items ? `<div class="items">${items}</div>` : ''}${visual}
  </article>`
}

export function buildDiagramHtml(diagram: Diagram) {
  const index = Object.fromEntries(diagram.nodes.map(node => [node.id, node]))
  const edges = diagram.edges.map(edge => {
    const from = index[edge.from], to = index[edge.to]; if (!from || !to) return ''
    const d = pathFor(edge, from, to), dash = edge.dashed ? ' dashed' : '', motion = edge.animation === 'none' ? '' : ' flow'
    return `<g class="edge${dash}" style="color:${esc(edge.color)}"><path class="rail" d="${d}"/><path class="signal${motion}" d="${d}" marker-end="url(#arrow)"/>${edge.label ? `<text><textPath href="#edge-${esc(edge.id)}" startOffset="50%">${esc(edge.label)}</textPath></text>` : ''}<path id="edge-${esc(edge.id)}" class="label-path" d="${d}"/></g>`
  }).join('')
  const dark = diagram.background !== 'light'
  return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${esc(diagram.title)}</title><style>
*{box-sizing:border-box}body{margin:0;background:${dark ? '#050709' : '#eef2f6'};color:${dark ? '#f5f7fa' : '#182231'};font-family:Inter,ui-sans-serif,system-ui,sans-serif}.page{min-width:${diagram.width + 84}px;padding:42px}.title{width:${diagram.width}px;margin:0 auto 24px}.title small{color:#86f34d;font:700 11px ui-monospace,monospace;letter-spacing:.14em}.title h1{margin:8px 0 4px;font-size:27px;letter-spacing:-.035em}.title p{margin:0;color:#7e8998;font-size:13px}.board{position:relative;width:${diagram.width}px;height:${diagram.height}px;margin:auto;overflow:hidden;border:1px solid ${dark ? '#242b32' : '#d4dce5'};border-radius:18px;background-color:${dark ? '#090c10' : '#fff'};background-image:linear-gradient(${dark ? '#151a20' : '#edf1f5'} 1px,transparent 1px),linear-gradient(90deg,${dark ? '#151a20' : '#edf1f5'} 1px,transparent 1px);background-size:28px 28px;box-shadow:0 30px 100px #0006}.edges{position:absolute;inset:0;width:100%;height:100%;z-index:1;overflow:visible}.edge path{fill:none}.rail{stroke:#3a444e;stroke-width:2}.signal{stroke:currentColor;stroke-width:2;filter:drop-shadow(0 0 4px currentColor)}.dashed .rail,.dashed .signal{stroke-dasharray:7 8}.flow{stroke-dasharray:8 16;animation:flow 2s linear infinite}.label-path{stroke:none}.edge text{fill:#8995a3;font-size:10px}.edge textPath{dominant-baseline:hanging}.node{position:absolute;z-index:2;display:flex;flex-direction:column;padding:16px;color:var(--ink);border:1px solid color-mix(in srgb,var(--tone),white 22%);border-radius:13px;background:color-mix(in srgb,var(--tone),transparent 1%);box-shadow:0 15px 42px #0008,inset 0 1px #ffffff10}.node.group{z-index:0;padding:19px 22px;border-style:dashed;border-color:#3d4852;background:color-mix(in srgb,var(--tone),transparent 4%);box-shadow:inset 0 0 80px #0007,0 20px 70px #0005}.node.platform:after{content:'';position:absolute;left:14px;right:14px;bottom:10px;height:3px;border-radius:3px;background:linear-gradient(90deg,#8cf354,#64cff5,transparent);opacity:.75}.node.actor,.node.metric{align-items:center;justify-content:center;border-radius:50%;text-align:center}.node.note{justify-content:center;border-color:#8050bc;background:linear-gradient(135deg,var(--tone),#11151a)}.node.technology,.node.electronic{justify-content:center;background:linear-gradient(135deg,var(--tone),#0b0f13)}.node.database{justify-content:center;padding-right:76px}.heading{position:relative;z-index:1;display:flex;min-width:0;align-items:flex-start;gap:9px}.technology .heading,.electronic .heading{align-items:center}.heading>div{min-width:0;flex:1}.heading i{display:grid;width:29px;height:29px;flex:0 0 29px;place-items:center;border:1px solid #45515c;border-radius:7px;color:#9ff56d;background:#0c1115;font:700 8px ui-monospace,monospace}.heading i.brand{width:39px;height:39px;flex-basis:39px;background:#090d10}.heading i.brand.electronic{color:#58d7ff}.heading i.brand svg{display:block;width:26px;height:26px}.heading em{display:block;margin-bottom:4px;overflow:hidden;color:#81dc57;font:700 7px ui-monospace,monospace;letter-spacing:.13em;white-space:nowrap;text-overflow:ellipsis;font-style:normal}.heading h2{margin:0;overflow:hidden;color:var(--ink);font-size:13px;line-height:1.2;letter-spacing:-.02em;white-space:nowrap;text-overflow:ellipsis}.group>.heading{width:100%;padding-bottom:11px;border-bottom:1px solid #2c353d}.group>.heading h2{font-size:15px}.heading b{padding:4px 6px;border:1px solid #7c5ab1;border-radius:9px;color:#c6a5ff;background:#251c36;font-size:6px;letter-spacing:.05em}.node>p{position:relative;z-index:1;margin:7px 0 0;overflow:hidden;color:#87929d;font-size:9px;line-height:1.45}.technology>p,.electronic>p{margin-top:-7px;margin-left:48px;font:700 7px ui-monospace,monospace;text-transform:uppercase;letter-spacing:.07em}.items{position:relative;z-index:1;display:flex;gap:5px;margin-top:auto;padding-top:11px}.items span{min-width:0;flex:1;overflow:hidden;padding:6px 4px;border:1px solid #313a43;border-radius:5px;color:#9ca7b0;background:#0b0f13;text-align:center;text-overflow:ellipsis;white-space:nowrap;font-size:7px}.platform-visual{display:grid;grid-template-columns:repeat(3,1fr);gap:5px;height:48px;margin-top:11px;padding:7px;border:1px solid #313a42;border-radius:6px 6px 2px 2px;background:#090d11}.platform-visual i{border:1px solid #4c5761;border-radius:2px;background:linear-gradient(135deg,#222a31,#0e1317)}.platform-visual i:nth-child(2),.platform-visual i:nth-child(5){border-color:#825ad0;background:#2b2041}.database-stack{position:absolute;right:18px;top:19px;width:42px;height:52px}.database-stack i{position:absolute;left:0;width:42px;height:21px;border:1px solid #5c6d7b;border-radius:50%;background:#131a20;box-shadow:inset 0 -6px #0a0f13}.database-stack i:nth-child(2){top:13px}.database-stack i:nth-child(3){top:26px;border-color:#82dd59}.group-grid{position:absolute;inset:76px 15px 15px;border:1px solid #1c242b;border-radius:9px;background-image:radial-gradient(#29323a 1px,transparent 1px);background-size:14px 14px;opacity:.48}.metric .heading{display:block}.metric .heading i{display:none}.metric .heading h2{font-size:25px;color:#9af565}.metric>p{text-align:center}.motion-pulse{animation:pulse 2.2s ease-in-out infinite}.motion-glow{animation:glow 2s ease-in-out infinite}@keyframes flow{to{stroke-dashoffset:-48}}@keyframes pulse{50%{transform:scale(1.035)}}@keyframes glow{50%{filter:drop-shadow(0 0 14px #84f24b)}}@media(max-width:900px){body{overflow:auto}.page{padding:18px}}
</style></head><body><main class="page"><header class="title"><small>DIAGRAMMATON · HTML ARTIFACT</small><h1>${esc(diagram.title)}</h1><p>${esc(diagram.subtitle)}</p></header><section class="board"><svg class="edges" viewBox="0 0 ${diagram.width} ${diagram.height}"><defs><marker id="arrow" markerWidth="8" markerHeight="8" refX="7" refY="4" orient="auto"><path d="M0 0L8 4L0 8z" fill="#86f34d"/></marker></defs>${edges}</svg>${diagram.nodes.map(renderNode).join('')}</section></main></body></html>`
}

export function downloadDiagramHtml(diagram: Diagram) {
  const blob = new Blob([buildDiagramHtml(diagram)], { type: 'text/html;charset=utf-8' }), url = URL.createObjectURL(blob), anchor = document.createElement('a')
  anchor.href = url; anchor.download = `${diagram.title.toLowerCase().replace(/[^a-z0-9]+/g, '-').replace(/^-|-$/g, '') || 'diagram'}.html`; anchor.click(); window.setTimeout(() => URL.revokeObjectURL(url), 1000)
}
